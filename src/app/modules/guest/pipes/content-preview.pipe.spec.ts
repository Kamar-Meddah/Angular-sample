import { ContentPreviewPipe } from './content-preview.pipe';

describe('ContentPreviewPipe', () => {
  it('create an instance', () => {
    const pipe = new ContentPreviewPipe();
    expect(pipe).toBeTruthy();
  });
});
